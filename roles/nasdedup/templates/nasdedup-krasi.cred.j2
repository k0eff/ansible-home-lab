# Managed by ansible (roles/nasdedup). Root-only, 0600.
# Values come from protected/inventories/group_vars/all.yaml — a read-only QNAP
# account, so a leak of this file cannot be used to change anything on the NAS.
username={{ nas_smb_user }}
password={{ nas_smb_password }}
