global:
  scrape_interval: 15s
  evaluation_interval: 15s

# koeff-ai-stack's LLM-routing services (litellm, headroom, qdrant, ollama,
# postgres) were decommissioned — only prometheus + grafana remain from this
# stack. No scrape targets left to declare; add real ones here if this
# prometheus instance ever gains something else to monitor.
scrape_configs: []
