FROM mcr.microsoft.com/playwright:latest

RUN npm install -g @playwright/mcp@latest

EXPOSE 8080

ENTRYPOINT ["playwright-mcp"]
CMD ["--host", "0.0.0.0", "--port", "8080", "--headless", "--no-sandbox", "--allowed-hosts", "*", "--isolated"]
