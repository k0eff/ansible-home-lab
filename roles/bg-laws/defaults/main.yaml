---
# bg-laws runtime on vm700 — written from bg-laws-pipeline/docs/spec/
# 80-queue-worker-admin.md §4 (the "vm700 runtime contract"); scheduling
# values below are that section's defaults, overridable here.

# Service account (§4.1): no sudo, owns everything under /opt/bg-laws.
bg_laws_user: bglaws
bg_laws_root: /opt/bg-laws

# Pinned bun, official linux-x64 tarball → /usr/local/bin/bun (§4.1).
# 1.3.14 is what the operator laptop runs — parity, not "latest".
bg_laws_bun_version: "1.3.14"

# Private repos, cloned over ssh with the same deploy key convention the
# ops-settings role already uses for helmfile-home-lab (protected/keys/
# docker/homelab-docker.key). The key VALUE lives only in the protected
# repo; only the path is referenced here.
bg_laws_pipeline_repo: "git@github.com:k0eff/bg-laws-pipeline.git"
bg_laws_data_repo: "git@github.com:k0eff/bg-laws.git"

# Daily incremental + admin rebuild (§4.4), local time.
bg_laws_cron_minute: "10"
bg_laws_cron_hour: "6"

# §4.3: NO LLM key on vm700 in v1 — the worker (laptop) holds the key and
# drains the queue; vm700 always enqueues. Referenced-but-empty so the env
# file shape is already right if that decision is ever reversed. A real
# value would belong in protected/inventories/group_vars/all.yaml, never here.
bg_laws_openrouter_api_key: ""
