{
  "id": "023",
  "branch": "main",
  "name": "core--bg-laws-deploy",
  "category": "core",
  "description": "Deploys the bg-laws pipeline runtime to vm700 per bg-laws-pipeline/docs/spec/80-queue-worker-admin.md section 4: bglaws user (no sudo), bun from the official tarball at /usr/local/bin/bun, git clones of bg-laws-pipeline and the bg-laws data repo under /opt/bg-laws, the var/queue + var/admin state dirs, a 0600 env file (no LLM key on the host — OPENROUTER_API_KEY referenced but empty by default), backfill.sh + incremental.sh wrappers sharing one flock file, a daily incremental cron in /etc/cron.d, and logrotate for /opt/bg-laws/logs. The backfill itself is NOT started by the role — it is operator-started inside tmux, once.",
  "implementation": {
    "file": "roles/bg-laws/tasks/main.yaml",
    "change": "New role bg-laws (tasks, defaults, templates for env/backfill.sh/incremental.sh/start-backfill.sh/cron/logrotate), wired into the ops play of playbook-index.yaml under tag bg_laws. Private repos are cloned over ssh with the existing protected/keys/docker/homelab-docker.key convention (same key the ops-settings role uses for helmfile-home-lab) — no new secret mechanism. Repo updates are fetch + ff-only merge under flock -n so a running backfill is never yanked out from under itself; the data repo is never merged at all (the runner owns that tree, spec 4.7)."
  },
  "validation": {
    "type": "live",
    "checks": [
      "MEASURED 2026-08-25: playbook run against vm700 completes green (ok=22, failed=0) with the clone slice skipped as pending-secret",
      "MEASURED 2026-08-25: /usr/local/bin/bun --version on vm700 prints 1.3.14, exit 0 (pinned to the operator laptop's version)",
      "MEASURED 2026-08-25: bglaws user exists uid=1001, no sudo group; /opt/bg-laws layout + var/queue/{pending,leases,done,failed} + var/admin all bglaws:bglaws",
      "MEASURED 2026-08-25: env is 0600 with OPENROUTER_API_KEY empty; backfill.sh/incremental.sh/start-backfill.sh installed 0755; /etc/cron.d/bg-laws carries the flock-guarded 10 6 * * * line; logrotate stanza parses clean under logrotate -d",
      "PENDING-SECRET: repo clones + bun install + 'bun run src/cli/main.ts version' on vm700 — protected/keys/docker/homelab-docker.key (RSA SHA256:N1sCBBXMj4OTbcP3lugqdm4JCX7UT9Lyhu9IGVI/0UU) is not registered to ANY GitHub identity (verified with ssh -F /dev/null from the laptop: Permission denied; the apparent 'Hi k0eff!' came from the laptop's own agent/config keys). The role probes access and skips the slice loudly instead of failing",
      "OPERATOR ACTION: add protected/keys/docker/homelab-docker.key.pub to GitHub as an SSH key of an account with read+write on k0eff/bg-laws-pipeline and k0eff/bg-laws (account-level, not per-repo deploy key — deploy keys are unique per repo and this same key must also keep serving the helmfile clones), then re-run: OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES ansible-playbook -i protected/inventories/inventory-main.yaml playbook-index.yaml -l ops --tags bg_laws",
      "NOTE: 'bun run src/cli/main.ts --help' exits 1 BY DESIGN (E_USAGE when no command); the role's smoke check uses 'version', which exits 0 — verified on the laptop checkout",
      "KNOWN LIMIT: static serving of /opt/bg-laws/var/admin (spec 80 §3.4/§4.7 item 7) not wired — needs a caddy edge change, tracked separately"
    ]
  },
  "status": "pending"
}
