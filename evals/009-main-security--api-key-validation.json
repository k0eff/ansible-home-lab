{
  "id": "009",
  "name": "security--api-key-validation",
  "category": "security",
  "description": "API keys validated before reaching LiteLLM backend",
  "implementation": {
    "file": "roles/ops-settings/templates/headroom/docker-compose.yml.j2",
    "change": "Headroom validates keys with HEADROOM_MODE: token and passes Authorization header to upstream"
  },
  "validation": {
    "type": "auth_header",
    "checks": [
      "Headroom runs in token mode (HEADROOM_MODE=token)",
      "Requests with missing API key are rejected by Headroom",
      "Valid keys are forwarded to ANTHROPIC_TARGET_API_URL (nginx-ingest)",
      "Authorization header format: Bearer <key> is preserved"
    ]
  },
  "status": "disabled",
  "branch": "main",
  "disabled_reason": "Superseded 2026-09-20: koeff-ai-stack is stopped (koeff_ai_stack_state: absent, eval 029). This eval validates a service that no longer runs — the LLM-routing chain (litellm, headroom, nginx-ingest, qdrant, ollama, postgres) was decommissioned 2026-08-31 and the prometheus + grafana pair that outlived it was stopped 2026-09-20. Several of these evals also point at template files that were deleted with those services. Re-enable only if the stack is restored."
}
