{
  "id": "010",
  "name": "security--auth-headers",
  "category": "security",
  "description": "Auth headers properly passed through nginx-ingest to Headroom",
  "implementation": {
    "file": "roles/ops-settings/templates/headroom/nginx-ingest.conf.j2",
    "change": "nginx-ingest proxy passes Authorization headers to Headroom without modification"
  },
  "validation": {
    "type": "proxy_header",
    "checks": [
      "nginx proxy_pass_request_headers is on",
      "Authorization header is not stripped by nginx",
      "Request headers are forwarded to http://headroom:8787",
      "Response from Headroom includes cache headers"
    ]
  },
  "status": "disabled",
  "branch": "main",
  "disabled_reason": "Superseded 2026-09-20: koeff-ai-stack is stopped (koeff_ai_stack_state: absent, eval 029). This eval validates a service that no longer runs — the LLM-routing chain (litellm, headroom, nginx-ingest, qdrant, ollama, postgres) was decommissioned 2026-08-31 and the prometheus + grafana pair that outlived it was stopped 2026-09-20. Several of these evals also point at template files that were deleted with those services. Re-enable only if the stack is restored."
}
