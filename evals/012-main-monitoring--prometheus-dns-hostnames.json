{
  "id": "012",
  "name": "monitoring--prometheus-dns-targets-with-health",
  "category": "monitoring",
  "description": "Prometheus configured with DNS scrape targets and health check validation",
  "implementation": {
    "files": [
      "roles/ops-settings/templates/headroom/prometheus.yml.j2",
      "roles/ops-settings/templates/headroom/docker-compose.yml.j2"
    ],
    "changes": [
      "Replace Docker service names with fully qualified DNS hostnames (litellm.local.koeff.com, headroom.local.koeff.com, qdrant.local.koeff.com)",
      "Add Prometheus healthcheck to docker-compose: /-/healthy endpoint with 10s interval, 5s timeout, 3 retries"
    ]
  },
  "validation": {
    "type": "config_and_health",
    "checks": [
      "prometheus.yml contains DNS targets: litellm.local.koeff.com:4000, headroom.local.koeff.com:8787, qdrant.local.koeff.com:6333",
      "No Docker service names remain in scrape_configs",
      "Prometheus healthcheck defined in docker-compose.yml with /-/healthy endpoint",
      "Prometheus healthcheck interval=10s, timeout=5s, retries=3",
      "Prometheus container reaches healthy state when all targets connect",
      "All scrape targets report state=up in Prometheus API"
    ]
  },
  "status": "disabled",
  "branch": "main",
  "disabled_reason": "Superseded 2026-09-20: koeff-ai-stack is stopped (koeff_ai_stack_state: absent, eval 029). This eval validates a service that no longer runs — the LLM-routing chain (litellm, headroom, nginx-ingest, qdrant, ollama, postgres) was decommissioned 2026-08-31 and the prometheus + grafana pair that outlived it was stopped 2026-09-20. Several of these evals also point at template files that were deleted with those services. Re-enable only if the stack is restored."
}
