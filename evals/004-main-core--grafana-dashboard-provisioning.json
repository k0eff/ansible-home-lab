{
  "id": "004",
  "name": "core--grafana-dashboard-provisioning",
  "category": "core",
  "description": "Grafana dashboards provisioned in data volume directory",
  "implementation": {
    "file": "roles/ops-settings/tasks/headroom.yaml",
    "change": "Copy rendered dashboard JSON files to /var/lib/docker-data/koeff-ai-stack/grafana/dashboards/"
  },
  "validation": {
    "type": "file_exists",
    "checks": [
      "Task 'Ensure Grafana dashboards data directory' creates /var/lib/docker-data/koeff-ai-stack/grafana/dashboards",
      "Task 'Copy dashboard files to Grafana data volume' copies grafana-dashboard-*.json files",
      "Grafana datasources config references provisioning dashboards directory"
    ]
  },
  "status": "disabled",
  "branch": "main",
  "disabled_reason": "Superseded 2026-09-20: koeff-ai-stack is stopped (koeff_ai_stack_state: absent, eval 029). This eval validates a service that no longer runs — the LLM-routing chain (litellm, headroom, nginx-ingest, qdrant, ollama, postgres) was decommissioned 2026-08-31 and the prometheus + grafana pair that outlived it was stopped 2026-09-20. Several of these evals also point at template files that were deleted with those services. Re-enable only if the stack is restored."
}
